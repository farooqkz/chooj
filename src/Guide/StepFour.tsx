function StepFour() {
  return (
    <div className="step">
      <p>
        You can use chooj with an existing account on some homeserver to chat in
        different rooms on the Matrix network. If you haven't got an account,
        don't worry! You can use guest access through a homeserver to join rooms
        which allow guest access.
      </p>
    </div>
  );
}

export default StepFour;
